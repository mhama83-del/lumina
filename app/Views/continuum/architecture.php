<?= $this->extend('continuum/_layout') ?>
<?= $this->section('content') ?>
<div class="card"><h2>How it works</h2>
<pre class="small">Career Passport / profile reference (mock adapter)
   → Evidence profile → Role Context version → Consent snapshot
   → Application event stream → Review, outcome & intervention</pre>
<p class="small muted">Talentbank Passport integration is a mock adapter. A real API is
<strong>REQUIRES TALENTBANK VALIDATION</strong>.</p></div>
<?= $this->endSection() ?>
